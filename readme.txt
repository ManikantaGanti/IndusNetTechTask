1. Clone the App using the provided git link
2. Node.js application is required to run the project
3. Open the terminal and set the current directory of it as the project root directory and run "npm i" command to install the node modules required
4. Finally "node server.js" command is used to start the project and now open the "homepage.html" file in the browser using it's path to start using the project